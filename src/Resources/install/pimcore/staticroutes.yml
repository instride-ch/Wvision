routes:
  app_search:
    pattern: '/(\\w+)\\/search/'
    reverse: '/%_locale/search'
    module: AppBundle
    controller: '@AppBundle\Controller\SearchController'
    action: search
    variables: _locale
    priority: 1